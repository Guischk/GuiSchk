import colors from './colors';
import standard from './standard';

const theme = {
  fonts: {
    sans: 'system-ui, helvetica',
    mono: '"Fira Mono", mono',
  },
  ...standard,
  colors: colors,
};

export default theme;
